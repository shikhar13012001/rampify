# Rampify — Weekly Report

> Companion to `STATUS.md` / `WORKING_AGREEMENT.md` / `METRICS.md`. Generated by
> `scripts/generate-weekly-report.mjs` — regenerate after refreshing
> `validation-data/` (see `validation-data/README.md`), don't hand-edit.

**Window**: 2026-09-02 to 2026-09-09 (inclusive, UTC). Generated: 2026-09-09T16:20:16.771Z.

## No analytics data available for this period

`validation-data/analytics-events.json` does not exist. Nothing below is fabricated to fill this gap, per `WORKING_AGREEMENT.md` §6 — run:

```
node --env-file=.env.local scripts/export-analytics-events.mjs --since 2026-09-02 --until 2026-09-09
node scripts/generate-weekly-report.mjs --since 2026-09-02 --until 2026-09-09
```

then re-run this script. See `validation-data/README.md` for the exact expected files.

## Search Console

**Window caution**: Search Console data typically lags 1-3 days. The requested window ends 2026-09-09 (0.7 days ago) — treat this window as potentially incomplete, not a final number, until re-checked after it fully ages.

No Search Console export found (`validation-data/search-console-queries.csv` / `-pages.csv` / `-totals.csv`). See `validation-data/README.md` for how to produce these from the Search Console UI.

## Recommendation — which source or funnel stage deserves the next experiment?

**No recommendation — insufficient data.** There is no qualified-session data for this window to rank sources or funnel stages against each other. Run `scripts/export-analytics-events.mjs` after real traffic exists, then re-generate this report, before asking this question again.

This recommendation is a heuristic (largest-volume vs. lowest-activation-rate-with-minimum-volume),
not a statistical significance test — treat it as a starting hypothesis for the experiment log
in `docs/validation/LAUNCH.md` §6, not a conclusion.

